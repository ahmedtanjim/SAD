//datatype?variableName;[Way of declaring a nullable type]
//A nullable type in Dart null safety is declared by placing a ? after the data type, allowing the variable to hold either a value or null.
void main() {
  int? age;
  age = 21;
  String? name;
  print(name);
  print(age);
}
